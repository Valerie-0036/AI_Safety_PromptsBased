define flow
    user ...
    $agent_response = execute langgraph_agent_action(user_input=$last_user_message)
    bot $agent_response

define user ask agent question
    "What time is it?"
    "Tell me the current time."